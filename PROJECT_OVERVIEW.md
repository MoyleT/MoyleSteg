# Moyle AES-256 PNG Steganography Tool：v1.0 基线介绍（历史资料）

> 本文记录首次导入的 v1.0 CLI 基线，不代表当前桌面发布的完整功能或资源限制。当前桌面版为 1.4.1；使用说明见 `README_DESKTOP.md`，当前变更、测试范围与合成截图见 `docs/RELIABILITY_1.4.1.md`。其余版本化文档保留对应历史记录。`SHA256SUMS.txt` 仅用于原始基线溯源，当前发布以包内 `RELEASE_MANIFEST.json` 为准。

> 当前版本：1.0  
> 项目形态：本地离线 Python CLI 工具  
> 运行环境：Python 3.10+；Windows、macOS、Linux  
> 核心依赖：`cryptography`、`Pillow`  
> 当前主程序：`png_steg_aes256.py`

## 1. 项目摘要

本项目是一个面向任意文件的**认证加密与 PNG 像素隐写工具**。它将秘密文件作为不透明二进制数据处理，先选择性压缩，再使用 AES-256-GCM 进行认证加密，随后使用独立派生的布局密钥，把密文随机分散到 PNG 图像 RGB 通道的最低有效位中。解码过程完成像素提取、AES-GCM 认证、解密、可选解压与 SHA-256 完整性校验，最终恢复与原文件逐字节一致的结果。

项目同时提供两套工作流：

1. **独立 AES-256 文件加密**：生成本工具专用的 `.saes` 认证加密容器；
2. **密钥、加密、隐写、解码三合一**：生成普通可显示的隐写 PNG，并通过相同凭据恢复内部文件。

项目解决的是两类不同问题：

- **加密层**：即使隐藏数据被发现，没有正确凭据也无法读取或无声篡改内容；
- **隐写层**：秘密数据不以普通附件、连续文件尾或明文形式出现，而是编码在图片像素中。

本项目不是 JPEG 频域隐写、不是把 ZIP 直接拼接在图片末尾的 polyglot 文件，也不承诺专业取证环境下“绝对无法检测”。

---

## 2. 核心功能

### 2.1 认证加密

- 使用 `AES-256-GCM`；
- 同时提供机密性、完整性与认证；
- 错误口令、错误密钥、密文损坏或被使用像素位发生变化时，认证失败；
- 文件名、文件大小、压缩状态、SHA-256 和文件内容均位于加密区；
- 使用成熟的 `cryptography.hazmat.primitives.ciphers.aead.AESGCM`，不自行实现 AES。

### 2.2 两种凭据模式

- **256-bit 密钥文件模式**：系统安全随机源生成 32 字节主密钥，保存为 `.stegkey`；
- **口令模式**：口令经 scrypt 派生主密钥，参数为 `N=32768, r=8, p=1`。

实际秘密不会硬编码进程序，也不会嵌入 PNG。程序只包含密钥生成、派生、加密、布局、解码和校验逻辑。

### 2.3 密钥分离

主密钥经 HKDF-SHA256 派生 64 字节材料：

- 前 32 字节：AES-256-GCM 加密密钥；
- 后 32 字节：像素位置布局密钥。

因此，加密和位置选择不会直接复用同一组密钥字节。

### 2.4 随机全图分散

- 密文主体不从图片左上角连续写入；
- 通过 HMAC-SHA256 计数器随机源生成确定性位置；
- 采用分块、分层、无重复的位置选择；
- 默认逻辑块大小为 4096 个 RGB 通道；
- 相同图片、相同头部与正确凭据可以重建相同位置序列；
- Alpha 通道始终保持不变。

### 2.5 完整恢复

- 解密后检查内部格式；
- 检查声明长度；
- 必要时受限解压 zlib 数据；
- 重新计算 SHA-256；
- 恢复结果必须与加密前记录的大小和摘要一致；
- 未通过认证或校验的数据不会作为正常结果输出。

### 2.6 自动扩容

当载体图片容量不足时，可以按原始宽高比放大图片：

- 使用 Lanczos 重采样；
- 可设置自动放大后的最大容量占用率；
- 可设置最大像素数；
- 默认最大像素数为 150,000,000；
- 自动扩容解决容量问题，不等同于提升抗隐写分析能力。

### 2.7 独立加密容器

`encrypt` / `decrypt` 工作流不依赖图片，输出 `.saes` 文件。该容器沿用同一公开头、密钥派生和内部载荷结构，但把 AES-GCM 密文直接放在头部之后。

---

## 3. 项目边界

### 3.1 项目能够提供的性质

在当前实现和正确使用条件下，项目可以提供：

- 正确凭据下恢复原文件；
- 错误凭据无法通过 AES-GCM 认证；
- 对密文与认证头部篡改进行检测；
- 隐藏文件名和文件内容；
- 将主体密文随机分散到全图 RGB 通道；
- 保持 Alpha 通道值不变；
- 输出仍是可由普通图片查看器打开的 PNG；
- 保存隐写 PNG 后自动回读、解密和校验；
- 默认拒绝覆盖输入文件或已有输出。

### 3.2 项目不提供的保证

项目不能合理声称：

- 隐写绝对无法被发现；
- 固定格式不会被识别；
- 弱口令无法被离线猜测；
- PNG 被缩放、转码、裁剪或压缩后仍可恢复；
- 密钥丢失后存在后门或恢复渠道；
- 自定义 `.saes` 与其他加密软件兼容；
- 已通过正式第三方密码学审计；
- 适合把数 GB、数十 GB 文件隐写进单张普通图片。

---

## 4. 数据处理流程

### 4.1 隐写生成流程

```text
秘密文件
  ↓
读取原始字节并计算 SHA-256
  ↓
尝试 zlib level 9 压缩
  ↓
只在压缩结果更小时采用压缩数据
  ↓
构造内部明文载荷
  ↓
通过随机密钥文件或口令获得主密钥
  ↓
HKDF-SHA256 派生加密密钥与布局密钥
  ↓
生成 16 字节 salt 和 12 字节 AES-GCM nonce
  ↓
AES-256-GCM 认证加密
  ↓
构造 54 字节公开引导头
  ↓
引导头顺序写入固定 RGB LSB 区域
  ↓
密文按布局密钥分散写入其余 RGB LSB
  ↓
保存临时 PNG
  ↓
从临时 PNG 重新提取、认证、解密和校验
  ↓
验证通过后原子替换为正式输出
```

### 4.2 解码恢复流程

```text
读取隐写 PNG
  ↓
从固定位置读取 54 字节引导头
  ↓
解析格式版本、凭据模式、KDF 参数、salt、nonce、密文长度
  ↓
根据正确口令或密钥文件派生两把子密钥
  ↓
用布局密钥重建密文位置序列
  ↓
提取 AES-GCM 密文
  ↓
使用公开头核心字段作为 AAD 完成认证与解密
  ↓
解析文件名、大小、压缩方式和 SHA-256
  ↓
必要时解压
  ↓
验证长度和 SHA-256
  ↓
原子写入恢复文件
```

### 4.3 独立 `.saes` 流程

```text
秘密文件
  ↓
内部载荷构造、密钥派生、AES-256-GCM 加密
  ↓
54 字节公开头 + AES-GCM 密文
  ↓
.saes 文件
```

---

## 5. 密码学设计

### 5.1 AES-256-GCM

项目使用 32 字节加密密钥。GCM 的 16 字节认证标签由 `AESGCM.encrypt()` 自动附加在密文尾部。解密时，只要密文、AAD、nonce 或密钥不匹配，`AESGCM.decrypt()` 就会抛出认证错误。

### 5.2 Salt 与 nonce

每次加密操作通过 `os.urandom()` 生成：

- 16 字节随机 salt；
- 12 字节随机 nonce。

因此，即使使用相同载体、相同秘密文件和相同凭据，两次输出通常也不同：派生材料、AES-GCM 密文和像素位置都会随 salt 或 nonce 改变。

### 5.3 密钥文件格式

`.stegkey` 是一个两行 ASCII 文本文件：

```text
PNG-STEG-AES256-KEY-V1
<URL-safe Base64 编码的 32 字节随机密钥>
```

Base64 只是编码，不提供额外加密。持有密钥文件即持有解密能力。程序在支持的系统上尝试将其权限设置为 `0600`。

### 5.4 口令派生

口令模式使用：

```text
scrypt
N = 2^15 = 32768
r = 8
p = 1
输出长度 = 32 字节
```

scrypt 增加离线猜测成本，但不能把弱口令变成真正随机的 256-bit 秘密。口令安全性仍取决于长度与不可预测性。

### 5.5 HKDF 子密钥分离

HKDF 参数：

```text
算法：SHA-256
输出：64 字节
salt：当前容器的随机 salt
info：PNG-STEG-AES256/v1/enc-and-layout
```

输出按 32+32 字节拆分为加密密钥与布局密钥。

### 5.6 布局随机源

位置生成采用 HMAC-SHA256 计数器随机源。不同分块使用包含块编号的上下文：

```text
PNG-STEG-AES256/layout-block/<block index>
```

块内通过部分 Fisher–Yates 选择所需位置，保证同一块内部无重复；各块范围彼此不重叠。

---

## 6. 自定义格式

### 6.1 公开引导头

公开引导头固定为 54 字节，由核心字段和 CRC32 构成。核心结构为：

```python
struct.Struct(">8sBBBBBB16s12sQ")
```

字段包括：

| 字段 | 含义 |
|---|---|
| Magic | `SGAES001` |
| Version | 格式版本，当前为 1 |
| Mode | 口令模式或密钥文件模式 |
| scrypt log N | 口令模式参数 |
| scrypt r | 口令模式参数 |
| scrypt p | 口令模式参数 |
| Layout version | 布局版本，当前为 1 |
| Salt | 16 字节 |
| Nonce | 12 字节 |
| Ciphertext length | 64-bit 密文长度 |
| CRC32 | 4 字节快速结构校验 |

头部核心字段作为 AES-GCM 的 AAD，因此 CRC32 不是唯一的完整性保护。CRC32 用于快速识别格式损坏，AES-GCM 负责密码学认证。

公开头会暴露格式标识、模式、KDF 参数、salt、nonce 和密文长度，但不暴露内部文件名或明文内容。

### 6.2 内部加密载荷

解密后的内部固定结构为：

```python
struct.Struct(">4sBBHQQ32s")
```

内容包括：

- 内部 Magic：`PAY1`；
- 内部版本：1；
- 压缩方式：无压缩或 zlib；
- UTF-8 文件名长度；
- 原始文件大小；
- 存储数据大小；
- 原文件 SHA-256；
- UTF-8 基础文件名；
- 原始或压缩后的文件内容。

程序只保存文件基础名，不保存绝对路径。文件名会进行安全检查，拒绝 `/`、`\`、空字节、`.`、`..` 等危险值。

### 6.3 `.saes` 容器

`.saes` 的物理结构为：

```text
54 字节公开头
+
AES-256-GCM 密文和认证标签
```

这是项目私有格式，不是通用行业标准。

---

## 7. PNG 像素隐写模型

### 7.1 像素表示

输入图片由 Pillow 读取并转换为 8-bit RGBA。程序只操作 R、G、B 三个通道；Alpha 不参与嵌入。

逻辑 RGB 通道位置映射到 RGBA 原始字节：

```text
pixel   = logical_position // 3
channel = logical_position % 3
raw     = pixel * 4 + channel
```

每个嵌入位置仅修改最低有效位：

```text
new_value = (old_value & 0xFE) | bit
```

因此每个被使用的 RGB 通道数值最多变化 1。

### 7.2 容量公式

宽为 `W`、高为 `H` 时，总逻辑 RGB 通道数为：

```text
3 × W × H
```

54 字节引导头需要 432 个通道。可用 AES 密文容量为：

```text
floor((3 × W × H - 432) / 8) 字节
```

该容量是密文容量，不是秘密文件的精确最大体积。内部元数据、文件名与 16 字节 GCM 标签也会占用空间。

### 7.3 引导头与主体布局

- 前 432 个逻辑 RGB 通道用于固定引导头；
- 其余通道构成密文位置域；
- 密文位置由布局密钥确定；
- 主体按比例分散到各个 4096 通道块中；
- 不对全图创建一次性超大随机排列列表。

### 7.4 自动调整尺寸

自动扩容根据密文比特数、最大占用率和原图宽高比估算所需像素数，再使用 Lanczos 放大。程序还检查最大像素数，避免明显超出普通电脑资源范围。

自动放大只代表“能装下”。它不会自动使统计特征更自然，尤其是平滑插值区域可能对专业隐写分析不利。

---

## 8. CLI 功能表面

主程序提供以下子命令：

| 命令 | 作用 |
|---|---|
| `keygen` | 生成随机 256-bit `.stegkey` |
| `capacity` | 查看图片可用 AES 密文容量 |
| `encrypt` | 将普通文件加密为 `.saes` |
| `decrypt` | 解密 `.saes` |
| `hide` | 加密文件并随机分散隐写到 PNG |
| `info` | 使用正确凭据验证隐写图片并读取内部信息 |
| `extract` | 从 PNG 提取、认证、解密并恢复文件 |

凭据参数互斥：

```text
--key-file
--new-key-file
--password
```

不直接提供凭据参数时，程序通过终端交互输入口令或密钥文件路径。显式 `--password` 可能进入 shell 历史或进程参数，安全性低于交互输入。

主要文件与容量参数：

```text
--auto-resize
--max-fill
--max-pixels
--force
--force-key
```

---

## 9. 典型使用示例

### 9.1 安装依赖

```powershell
py -m pip install -r .\requirements.txt
```

### 9.2 生成密钥文件

```powershell
py .\png_steg_aes256.py keygen .\private.stegkey
```

### 9.3 生成三合一隐写 PNG

```powershell
py .\png_steg_aes256.py hide `
  .\cover.png `
  .\secret.pdf `
  .\hidden.png `
  --key-file .\private.stegkey
```

### 9.4 自动生成新密钥并隐写

```powershell
py .\png_steg_aes256.py hide `
  .\cover.png `
  .\secret.pdf `
  .\hidden.png `
  --new-key-file .\secret.stegkey
```

### 9.5 容量不足时自动放大

```powershell
py .\png_steg_aes256.py hide `
  .\cover.png `
  .\secret.pdf `
  .\hidden.png `
  --new-key-file .\secret.stegkey `
  --auto-resize `
  --max-fill 0.60
```

### 9.6 验证和查看内部信息

```powershell
py .\png_steg_aes256.py info `
  .\hidden.png `
  --key-file .\secret.stegkey
```

### 9.7 提取文件

```powershell
py .\png_steg_aes256.py extract `
  .\hidden.png `
  .\recovered.pdf `
  --key-file .\secret.stegkey
```

### 9.8 独立 AES-256-GCM 加密

```powershell
py .\png_steg_aes256.py encrypt `
  .\secret.pdf `
  .\secret.pdf.saes `
  --new-key-file .\secret-aes.stegkey
```

### 9.9 解密 `.saes`

```powershell
py .\png_steg_aes256.py decrypt `
  .\secret.pdf.saes `
  .\recovered.pdf `
  --key-file .\secret-aes.stegkey
```

---

## 10. 工程结构

```text
Moyle_AES256_PNG_Steg_v1.0/
├── png_steg_aes256.py
├── README_AES256_三合一.md
├── PROJECT_OVERVIEW.md
├── requirements.txt
├── requirements-dev.txt
├── 安装依赖.bat
├── 运行测试.bat
├── SHA256SUMS.txt
└── tests/
    └── test_png_steg_aes256.py
```

### 文件职责

| 文件 | 作用 |
|---|---|
| `png_steg_aes256.py` | 主程序、格式实现、密码学、LSB 布局和 CLI |
| `README_AES256_三合一.md` | 面向用户的安装与使用说明 |
| `PROJECT_OVERVIEW.md` | 项目定位、架构、格式、安全边界与工程状态 |
| `requirements.txt` | 运行依赖 |
| `requirements-dev.txt` | 测试依赖 |
| `安装依赖.bat` | Windows 依赖安装入口 |
| `运行测试.bat` | Windows 测试入口 |
| `tests/test_png_steg_aes256.py` | 自动化测试 |
| `SHA256SUMS.txt` | 包内关键工程文件哈希 |

当前项目不包含 GUI、安装器、独立可执行文件、CI 配置、Python 包发布元数据或正式许可证文件。

---

## 11. 自动化测试范围

现有测试集覆盖 10 项关键行为：

1. 随机生成的密钥文件可往返读取，并确实包含 32 字节密钥；
2. 随机布局位置唯一，并覆盖完整位置域；
3. 口令模式能够完整隐写并恢复任意字节；
4. 错误口令被 AES-GCM 拒绝；
5. 密钥文件模式能够正确往返恢复；
6. 小载体容量不足时能够自动放大并满足占用率目标；
7. 隐写过程不会改变 Alpha 通道；
8. 独立 `.saes` 文件能够完成加密和解密；
9. 修改一个实际使用的主体位会破坏 AES-GCM 认证；
10. 相同输入和凭据重复生成时得到不同输出，且两者都可正确解码。

测试入口：

```powershell
py -m pytest -q
```

或在 Windows 上运行：

```text
运行测试.bat
```

---

## 12. 文件写入与可靠性设计

### 12.1 隐写 PNG

隐写图片先写入同目录临时文件。程序随后重新打开临时 PNG，按同样凭据完成提取、AES-GCM 认证和 SHA-256 校验。只有自检通过，才使用 `os.replace()` 替换正式输出。

### 12.2 `.saes` 与恢复文件

普通加密文件和恢复文件使用临时文件、`flush()`、`fsync()` 与 `os.replace()` 完成原子写入，降低程序中断后留下半写入正式文件的概率。

### 12.3 覆盖保护

程序默认阻止：

- 隐写输出覆盖载体图片；
- 隐写输出覆盖秘密源文件；
- 加密输出覆盖源文件；
- 提取结果覆盖隐写 PNG；
- 解密结果覆盖 `.saes`；
- 未指定 `--force` 时覆盖已有结果；
- 未指定 `--force-key` 时覆盖已有新密钥文件。

---

## 13. 安全模型与现实风险

### 13.1 密钥管理

密钥文件丢失意味着数据无法恢复；密钥文件泄露意味着持有者具备解密能力。密钥与隐写图片同时存放在同一个目录、磁盘或云端，会削弱“分离保存”的安全价值。

### 13.2 弱口令

AES-256 的算法强度不能补偿可预测口令。scrypt 只能增加猜测成本，不能防止对短口令、生日、姓名或常见字符串的离线字典攻击。

### 13.3 固定格式指纹

54 字节引导头固定写在图片前部 RGB LSB 中，并包含 `SGAES001`。了解本工具格式的人可以识别该头部和密文长度，尽管无法在没有凭据时读取加密内容。

### 13.4 LSB 脆弱性

PNG 被以下方式处理后，隐藏数据可能不可恢复：

- 转换成 JPEG；
- 社交平台重新压缩；
- 缩放、裁剪、旋转后重采样；
- 截图；
- 降噪、锐化、滤镜或颜色处理；
- 任何改变像素数值的图片优化。

AES-GCM 能检测损坏，但不能修复损坏。本版本没有错误纠正码、多副本冗余或跨图分片恢复。

### 13.5 隐写检测

专业隐写分析可能利用最低位统计、噪声模型、尺寸异常、文件体积变化或固定格式头识别异常。本项目的随机布局不等于“不可检测”。

### 13.6 输入解析与资源消耗

不可信图片会通过 Pillow 解码，可能带来超大像素数、压缩炸弹式资源消耗或解析器攻击面。程序设置最大像素数，但 1.5 亿 RGBA 像素本身仍可能消耗数百 MiB 以上内存。

### 13.7 明文生命周期

程序在加密前、解密后会把完整文件读入 Python 内存；恢复文件会经过临时文件写入。它不是针对内存取证、页面文件取证或高对抗多用户主机设计的无痕系统。

### 13.8 依赖供应链

项目依赖 `cryptography` 和 `Pillow`。安全性还取决于依赖来源、版本和运行环境。当前依赖文件指定最低版本，而不是完全锁定可重复构建版本。

---

## 14. 性能与资源边界

当前实现不是流式架构。以下对象可能同时完整存在于内存：

- 原始秘密文件；
- zlib 压缩结果；
- 内部明文载荷；
- AES-GCM 密文；
- 完整 RGBA 图片数据；
- 解密和恢复数据。

虽然像素布局按块生成，避免建立整个图片的全量随机位置列表，但文件内容和图像仍是全量内存处理。

代码中设置：

```text
MAX_DECODED_SIZE = 8 GiB
DEFAULT_MAX_PIXELS = 150,000,000
```

前者是安全解析上限，不代表普通电脑适合处理 8 GiB 载荷。实践上，本项目更适合 KB、MB 到中等几十 MB 级文件与合理尺寸 PNG，不适合数 GB 或 80 GB 级单图隐写。

---

## 15. 图像兼容性边界

输入可以是 Pillow 能识别的普通图片，但输出强制为 PNG。输入最终会转换为 8-bit RGBA，因此当前实现不承诺保留：

- 16-bit PNG 精度；
- 调色板结构；
- APNG/GIF 多帧；
- EXIF；
- ICC 色彩配置；
- DPI；
- PNG 文本块；
- 自定义 ancillary chunks；
- 其他非像素元数据。

主要保持的是：

- 可见像素内容；
- 图像宽高；
- Alpha 通道数值。

---

## 16. 当前未实现的能力

当前版本没有实现：

- 图形界面或拖拽界面；
- Windows 独立 EXE；
- 目录递归和多文件原生容器；
- 自动分卷到多张图片；
- 流式加密与流式图像处理；
- Reed–Solomon 等错误纠正码；
- 图片损坏后的局部恢复；
- 内容感知型纹理区域选择；
- JPEG DCT 或小波域隐写；
- 隐写分析评分；
- 原始 PNG 元数据完整保留；
- 多载荷共存、替换或删除；
- 密钥轮换与重新加密；
- 正式标准容器互操作；
- 云同步、自动备份或密钥托管；
- 第三方安全审计证明。

---

## 17. 功能契约

### 输入

```text
载体图片
任意类型秘密文件
随机 256-bit 密钥文件或用户口令
```

### 三合一输出

```text
普通可显示的 PNG
可选的新 .stegkey 文件
```

### 独立加密输出

```text
.saes 认证加密容器
可选的新 .stegkey 文件
```

### 正确性条件

```text
SHA-256(恢复文件) == SHA-256(原文件)
```

并且恢复文件与原文件逐字节一致。

### 失败条件

以下情况应返回错误而不是输出未经验证的数据：

- 凭据类型不匹配；
- 口令或密钥错误；
- AES-GCM 认证失败；
- 格式版本不支持；
- 头部 CRC32 错误；
- 声明长度超出图片或容器范围；
- 解压数据异常；
- 恢复长度不匹配；
- SHA-256 不匹配；
- 输出路径冲突。

---

## 18. 工程状态

当前项目是一个单主程序文件、一个测试文件和若干说明/依赖文件构成的 CLI 工具。主程序集中了密码学、格式、图像处理、文件 I/O 与命令行逻辑，便于单文件分发，但模块化程度有限。

当前发布版本的重点是：

- 明确、可复现的文件格式；
- 本地离线运行；
- 两种凭据模式；
- 认证加密；
- 密钥化随机布局；
- 完整性验证；
- 基础资源限制；
- Windows 友好的启动方式；
- 自动化回归测试。

项目尚未声明开源许可证。未附许可证时，不应默认推断具体再分发或衍生授权范围。

---

## 19. 隐私与发布包内容

工程发布包只包含代码、文档、依赖、批处理入口、测试与哈希，不包含：

- 用户的秘密 PDF 或其他载荷；
- 用户载体图片；
- 生成后的隐写成品；
- 真实 `.stegkey`；
- 用户口令；
- 任何恢复后的私人文件。

这使工程包可以与实际秘密数据分离保存和传递。

---

## 20. 一句话定义

> Moyle AES-256 PNG Steganography Tool 是一个离线、跨平台、面向单文件的认证加密与 PNG RGB-LSB 隐写工具：它使用 AES-256-GCM 保护秘密内容，使用 scrypt 或随机 256-bit 密钥建立主密钥，使用 HKDF 分离加密与布局用途，再通过 HMAC-SHA256 驱动的分块随机布局把密文分散到全图，并在恢复时执行认证、解压和 SHA-256 完整性校验。
